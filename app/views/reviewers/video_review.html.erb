<%= title "Choose Themes for Reviewed ~ KAPHOO" %>
<div class="col-md-8 brek skiw">
	<div class="wq87uy">
		<% @videoos.each do |video| %>
			<div id="kjhgt565">
				<%= render partial: 'video', locals: {video: video} %>
			</div>
		 <% end %> 
	</div>
</div>
<div class="col-md-4 brok kuio">
	<div class="for_reviewer">
		<div class="team kls">
			<div class="ti_l">
				<span>YOUR CHOOSES</span>
			</div>
		</div>
	</div>
	<div class="fresk">
		<div id="forever">
			<%= render @videofxes %>
		</div>
		<div class="clear"></div>
	</div>
	<div class="brn878">
		<%= link_to "CHECK OUT", project_path(current_user.team_id), class: "gdt35", "data-turbolinks": false %>
	</div>
</div>
<div class="clear"></div>
<script>
	$('.aktif').removeClass();
	$('.videos').addClass('aktif');
	<% @videoos.each do |video| %>
		$(document).ready(function(){
			$('#add_to_pro_video<%= video.id %>').click(function(){
			$.ajax({
				type: "GET",
				url: "<%= add_to_pro_video_path(video) %>",
				success: function(){
					$('#dhg789hjk_<%= video.id %>').hide("slow");
				}
			});
			});
		});
	 <% end %> 
</script>