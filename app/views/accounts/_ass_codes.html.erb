<% for code in @codes %>
  <div class="col-md-3 sol" id="code_del<%= code.id %>">
    <div class="soll soli">
      <div class="img_k">
        <%= image_tag code.thumbnail_url(:thumbnail), class: "isabela" %>
        <div class="hof hgsd54657">
          <div class="dhgft">
            <%= link_to item_code_path(code), class: "btyr-hfg", "data-turbolinks": false do %>
              <i class="fa fa-rocket" aria-hidden="true"></i>
            <% end %> 
          </div>
          <ul class="fhg">
            <% if code.freeitems.map{|free| free.code_id}.join("").to_i == code.id %>
              <li class="dhg734 kd64" style="border-radius: 0px;cursor:pointer;"><i style="color: #ff9c49;" class="fa fa-unlock-alt" aria-hidden="true"></i></li>
            <% else %>
              <li class="dhg734 kd64" onclick="codeFree(<%= code.id %>)" style="border-radius: 0px;cursor:pointer;"><i id="color_<%= code.id %>" style="color: #A1A1A1;" class="fa fa-unlock-alt" aria-hidden="true"></i></li>
            <% end %>
            <li class="dhg734 kd64 del_86" onclick="dellCodeAdmin(<%= code.id %>)"</li>
          </ul>
          <div class="tooltip_j">
            <div id="dat_tooltip">
              <div class="img_o">
                <% code.screenshots[0..0].each do |image| %>
                  <%= image_tag image.screenshot_url(:hover) %>
                <% end %>  
              </div>
              <div class="titlw pull-left">
                <h4 class="je"><%= truncate(code.name, length: 50) %></h4>
                <span class="by"><%= code.user.company %> | </span> 
                <span class="by">codes / <%= code.category.name %></span>
              </div>
              <div class="prce pull-right">
                <span class="k">$<%= code.price.to_i.floor %></span>
              </div>
              <div class="clear"></div>
            </div>
          </div>
        </div>
      </div>
      <div class="info">
        <ul class="list-inline lf pull-left">
          <li>
            <span class="icon-cart_3 icon_cart_3 js"></span><span class="dr"><%= code.sell %></span>
          </li>
          <li>
            <span class="icon-price icon_price_1 js"></span><span class="dr">$<%= code.price.to_i.floor %> / $<%= code.price_ext.to_i.floor %></span>
          </li>
        </ul>
        <div class="clear"></div>
      </div>
    </div>
  </div>
<% end %>
<script>
$(function(){
  $('.but').removeClass('active');
  $('.co_de').addClass('active');
});
</script>
