<% @videofxes.each do |video| %>
  <div class="col-md-3 sol" id="videofx_del<%= video.id %>">
    <div class="soll soli">
      <div class="img_k">
        <%= image_tag video.thumbnail_url(:thumbnail), class: "isabela" %>
        <div class="hof hgsd54657">
          <div class="dhgft">
            <%= link_to item_videofx_path(video), class: "btyr-hfg", "data-turbolinks": false do %>
              <i class="fa fa-rocket" aria-hidden="true"></i>
            <% end %>
          </div>
          <ul class="fhg">
            <% if video.freeitems.map{|free| free.videofx_id}.join("").to_i == video.id %>
              <li class="dhg734 kd64" style="border-radius: 0px;cursor:pointer;"><i style="color: #ff9c49;" class="fa fa-unlock-alt" aria-hidden="true"></i></li>
            <% else %>
              <li class="dhg734 kd64" onclick="videofxFree(<%= video.id %>)" style="border-radius: 0px;cursor:pointer;"><i id="color_<%= video.id %>" style="color: #A1A1A1;" class="fa fa-unlock-alt" aria-hidden="true"></i></li>
            <% end %>
            <li class="dhg734 kd64 del_86" onclick="dellVideoAdmin(<%= video.id %>)"></li>
          </ul>
          <div class="tooltip_j">
            <div id="dat_tooltip">
              <div class="img_o">
                <% video.videms.each do |videm| %>
                  <video id="ajjayyy" class="video-js aja_<%= videm.id %>" preload="auto" width="463" height="257" data-setup="{}" style="width: 463px !important; height: 257px !important">
                    <source src="<%= videm.trailer %>" type='video/mp4'>
                  </video>
                <% end %> 
              </div>
              <div class="titlw pull-left">
                <h4 class="je"><%= truncate(video.name, length: 50) %></h4>
                <span class="by"><%= video.user.company %> | </span> 
                <span class="by">Videofx / <%= video.category.name %></span>
              </div>
              <div class="prce pull-right">
                <span class="k">$<%= video.price.to_i.floor %></span>
              </div>
              <div class="clear"></div>
            </div>
          </div>
        </div>
      </div>
      <div class="info">
        <ul class="list-inline lf pull-left">
          <li>
            <span class="icon-cart_3 icon_cart_3 js"></span><span class="dr"><%= video.sell %></span>
          </li>
          <li>
            <span class="icon-price icon_price_1 js"></span><span class="dr">$<%= video.price.to_i.floor %> / $<%= video.price_ext.to_i.floor %></span>
          </li>
        </ul>
        <div class="clear"></div>
      </div>
    </div>
  </div>
<% end %>
<script>
$(function(){
  $('.but').removeClass('active');
  $('.vid_ex').addClass('active');
});
</script>
