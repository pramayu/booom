$('#unfollow_button').text('Follow')
                     .attr('class', '_follow_btn')
                     .attr('href', "/user/store/<%= @user.id %>/follow_user")
                     .attr('id', 'follow_button');

