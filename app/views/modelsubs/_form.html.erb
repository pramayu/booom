<%= form_for @modelsub do |f| %>
  <div class="col-md-8 brek brrroo">
    <div class="user_chooses">
      <div class="kate_kat">
        <h2>Add New Modelsub</h2>
        <p>configuration your store to convince buyers with the quality of your products</p>
      </div>
      <div class="sod data-acct">
        <div class="form-group fk">
          <span class="fl">Modelsub Name</span>
          <div class="fs">
            <%= f.text_field :name, class: "form-control sami", require: true %>
          </div>
        </div>
        <div class="form-group fk">
          <span class="fl">Subcategory for Item</span>
          <div class="fs">
            <%= f.collection_select(:subcategory_id, @subcategories, :id, :name, {prompt: "Choose subcategory"}, {class: "form-control sami"})%>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="col-md-4 brok prek">
    <div class="mnu_fuk">
      <div class="btn-pi">
        <%= f.submit "SUBMIT", class: "btn_update" %>
      </div>
    </div>
  </div>
<% end %>
